import type * as Stitches from '@stitches/react'

import { styled } from '~/theme'

export const Styled{{ pascalCase name }} = styled('div', {
  variants: {},

  defaultVariants: {},

  compoundVariants: [],
})

export type Styled{{ pascalCase name }}Variants = Stitches.VariantProps<typeof Styled{{ pascalCase name }}>
