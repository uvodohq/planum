import { Meta } from '@storybook/addon-docs'
import { ComponentName, Frame } from '../../components'
import { GradientGroup } from './gradient-groups'

<Meta title="Foundations/Gradient" />

<!--- Component documentation -->

<ComponentName withFoundationBackground>Gradient</ComponentName>

- [Overview](#overview)
- [Gradients](#shadow-levels)

## Overview

Gradients are used to add depth to the background of a component. They are used to create a sense of depth and to add visual interest to a component. For using gradients in the background of a component, you must add the "`backgroundImage`" to the css.

## Gradient colors

<GradientGroup />
