import { ArgsTable, Story, Canvas, Meta } from '@storybook/addon-docs'
import { Box, Notification, Button } from '../../..'
import { toast, Toaster } from '..'

<Meta title="Components/Toast" />

<!--- Component template -->
<!--
export const toastTemplate = (args) => <Toast {...args} /> -->

<!--- Component documentation -->

# Toast

- [Overview](#overview)
- [Props](#props)

## Overview

Toast field includes a label and a text field users can type text into. They typically appear in forms and dialogs.

## Variants

<Canvas>
  <Story name="States">
    <Box css={{ flex: 1 }}>
      <Notification message="We have e-mailed your password reset link!" />
      <Button
        onClick={() => toast('We have e-mailed your password reset link!')}
        css={{ mt: 20, display: 'block' }}>
        Make me a toast
      </Button>
    </Box>
    <Box css={{ flex: 1 }}>
      <Notification message="Your product has been deleted" width="mini" />
      <Button
        onClick={() => toast('Your product has been deleted')}
        css={{ mt: 20, display: 'block' }}>
        Make me a toast
      </Button>
    </Box>
    <Toaster />
  </Story>
</Canvas>
