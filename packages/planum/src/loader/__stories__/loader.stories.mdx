import { Flex } from 'src'

import Loader from '../loader'
import { ProgressLoader } from './progress'
import { ArgsTable, Story, Canvas, Meta } from '@storybook/addon-docs'

<Meta title="Components/Loader" component={Loader} />

<!--- Component template -->

export const loaderTemplate = (args) => <Loader {...args} />

<!--- Component documentation -->

# Loader

- [Overview](#overview)
- [Variants](#variants)
- [Props](#props)

## Overview

Loader component is used to show loading state.

<Canvas>
  <Loader />
</Canvas>

## Variants

### Sizes

<Canvas>
  <Story name="Sizes">
    <Loader />
    <Loader size="medium" />
  </Story>
</Canvas>

### Theme

<Canvas>
  <Story name="Theme">
    <Flex
      css={{
        flex: 1,
        gap: 32,
        p: 32,
        alignItems: 'center',
        background: 'black',
      }}>
      <Loader theme="light" />
      <Loader size="medium" theme="light" />
    </Flex>
  </Story>
</Canvas>

### Progress

<Canvas>
  <Story name="Progress">
    <Loader value={33} isIndeterminate={false} />
    <ProgressLoader size="medium" />
  </Story>
</Canvas>

## Props

<ArgsTable of={Loader} />
